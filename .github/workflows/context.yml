name: Context explained

on:
  push:

jobs:
  my-context:
    runs-on: ubuntu-latest
    steps:
      - name: "My Step" 
        run: |
          echo "ACTION: $MYACTION "
          echo "Ator: $MY_ACTOR "
        env:
          MYACTION: ${{github.action}}
          MY_ACTOR: ${{github.actor}}