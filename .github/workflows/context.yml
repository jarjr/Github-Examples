name: Context explained

on:
  push:

jobs:
  my-context:
    runs-on: unbuntu-latest
    steps:
      - name: "My Step" 
        run: echo "Hello $MYACTION "
        env:
          MYACTION: ${{github.action}}